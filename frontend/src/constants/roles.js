// The 10 personas selectable on the Your Role screen (Phase 2). Defined
// here rather than inline so the status strip's role pill (Phase 0) and
// the Your Role screen (Phase 2) share exactly one list.
export const ROLES = [
  'General citizen',
  'Farmer',
  'Driver',
  'Fisherman',
  'Commuter',
  'Office worker',
  'Outdoor worker',
  'Student',
  'Researcher',
  'Emergency official',
]

export const DEFAULT_ROLE = ROLES[0]
